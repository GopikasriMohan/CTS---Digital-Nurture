# Set the server port (optional)
server.port=8080

# Logging level (optional)
logging.level.org.springframework=INFO

# Disable Thymeleaf cache if using templates (optional)
spring.thymeleaf.cache=false

# CORS (Cross-Origin Resource Sharing) - allow Angular to connect
# Optional: only needed if your frontend is on a different port like 4200
# You’ll usually configure CORS in the controller instead
